/**
 * Variant Configuration Exports
 *
 * Main entry point for variant configuration in the frontend.
 */

// Server components
export { ServerVariantProvider } from './variant.server.provider';

// Client components and hooks
export { VariantProvider } from './variant.provider';
export type { VariantContextValue } from './variant.provider';
